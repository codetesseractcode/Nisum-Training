<?xml version="1.0" encoding="UTF-8"?>
<beans xmlns="http://www.springframework.org/schema/beans"
       xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
       xmlns:context="http://www.springframework.org/schema/context"
       xmlns:mvc="http://www.springframework.org/schema/mvc"
       xmlns:aop="http://www.springframework.org/schema/aop"
       xsi:schemaLocation="http://www.springframework.org/schema/beans
       http://www.springframework.org/schema/beans/spring-beans.xsd
       http://www.springframework.org/schema/context
       http://www.springframework.org/schema/context/spring-context.xsd
       http://www.springframework.org/schema/mvc
       http://www.springframework.org/schema/mvc/spring-mvc.xsd
       http://www.springframework.org/schema/aop
       http://www.springframework.org/schema/aop/spring-aop.xsd">

    <!-- Enable component scanning for controllers -->
    <context:component-scan base-package="com.nisum.controller"/>

    <!-- Enable MVC annotations -->
    <mvc:annotation-driven/>

    <!-- Static resources -->
    <mvc:resources mapping="/resources/**" location="/resources/"/>

    <!-- View resolver -->
    <bean class="org.springframework.web.servlet.view.InternalResourceViewResolver">
        <property name="prefix" value="/WEB-INF/views/"/>
        <property name="suffix" value=".jsp"/>
    </bean>

    <!-- Web scoped beans -->

    <!-- Request scope bean -->
    <bean id="requestBean" class="com.nisum.beans.ScopeBean" scope="request">
        <property name="scopeName" value="request"/>
        <aop:scoped-proxy/>
    </bean>

    <!-- Session scope bean -->
    <bean id="sessionBean" class="com.nisum.beans.ScopeBean" scope="session">
        <property name="scopeName" value="session"/>
        <aop:scoped-proxy/>
    </bean>

    <!-- Global session scope bean -->
    <bean id="globalSessionBean" class="com.nisum.beans.ScopeBean" scope="globalSession">
        <property name="scopeName" value="globalSession"/>
        <aop:scoped-proxy/>
    </bean>

</beans>
